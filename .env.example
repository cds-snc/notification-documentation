# API base URL for the dev server proxy (used when running locally with npm run dev)
# Defaults to https://api.notification.canada.ca if not set
API_BASE_URL=https://api.staging.notification.cdssandbox.xyz
